/**
 * @openapi
 * components:
 *   schemas:
 *     NoSaleReason:
 *       type: object
 *       required:
 *         - reason
 *       properties:
 *         id:
 *           type: string
 *         reason:
 *           type: string
 *         createdAt:
 *           type: string
 *           format: date-time
 *         updatedAt:
 *           type: string
 *           format: date-time
 */
