/**
 * @openapi
 * components:
 *   schemas:
 *     ClockingType:
 *       type: object
 *       required:
 *         - name
 *         - payMultiplier
 *       properties:
 *         id:
 *           type: string
 *         name:
 *           type: string
 *         payMultiplier:
 *           type: number
 *           default: 1.0
 *         createdAt:
 *           type: string
 *           format: date-time
 *         updatedAt:
 *           type: string
 *           format: date-time
 */
