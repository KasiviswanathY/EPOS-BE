/**
 * @openapi
 * components:
 *   schemas:
 *     DiscountReason:
 *       type: object
 *       required:
 *         - reason
 *       properties:
 *         id:
 *           type: string
 *         reason:
 *           type: string
 *         defaultValue:
 *           type: number
 *         createdAt:
 *           type: string
 *           format: date-time
 *         updatedAt:
 *           type: string
 *           format: date-time
 */
